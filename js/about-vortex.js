// 3D effect disabled - using simple banner background
(function () {
    var canvas = document.getElementById('about-vortex-canvas');
    if (!canvas) return;
    
    // Hide canvas - use CSS background instead
    canvas.style.display = 'none';
})();
